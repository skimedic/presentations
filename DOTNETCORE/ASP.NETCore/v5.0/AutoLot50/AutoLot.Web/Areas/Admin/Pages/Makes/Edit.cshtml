@page "{id?}"
@model EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Make</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.EditorFor(x => x.Make)
            <input type="hidden" asp-for="Make.Id" />
            <input type="hidden" asp-for="Make.TimeStamp" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" /> | 
                <item-list></item-list>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
